# Frozen production dependencies for compliance-portal foundation batch.
# Pinned for Python 3.12+. Regenerate after pyproject.toml changes.

fastapi==0.140.0
uvicorn[standard]==0.32.0
jinja2==3.1.6
pydantic==2.9.2
pydantic-settings==2.6.1
httpx==0.27.2
python-multipart==0.0.31
structlog==24.4.0
orjson==3.11.9
asyncpg==0.30.0
sqlalchemy==2.0.36
alembic==1.13.3
authlib==1.7.2
itsdangerous==2.2.0
cryptography==50.0.0
redis==5.2.0
slowapi==0.1.9
prometheus-client==0.21.0
starlette==1.3.1

# WI-19 — PDF Export Service
# Native deps required: cairo, pango, gdk-pixbuf (brew install cairo pango gdk-pixbuf libffi)
weasyprint==69.0
pikepdf==10.5.1
pyHanko==0.34.1
Brotli==1.2.0
cachetools==7.0.6

# WI-10 — Incident notes markdown rendering (AMD-19)
markdown-it-py==3.0.0
bleach==6.4.0

# Dev / test (kept here for reproducible CI; install with `pip install -r requirements.txt`)
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==6.0.0
ruff==0.7.4
mypy==1.13.0
respx==0.21.1
freezegun==1.5.1
fakeredis==2.26.1
