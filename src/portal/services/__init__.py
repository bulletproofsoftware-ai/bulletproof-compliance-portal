"""Service-layer helpers (SLA computation, markdown rendering, workflow validators).

These are pure-Python modules with no I/O — they encapsulate business rules
that would otherwise be duplicated across routers and templates.
"""
